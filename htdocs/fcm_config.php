<?php
// Firebase Cloud Messaging v1 (Service Account JSON)
// Place the service account JSON on the server and set the path here.
$fcmServiceAccountPath = __DIR__ . '/firebase-service-account.json';

// Optional: fallback to legacy server key (not recommended)
$fcmServerKey = '';
